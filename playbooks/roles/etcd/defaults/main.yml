---
# ============================================================================
# Role Defaults: etcd (etcd Database Health)
# ============================================================================
# Targets: Ansible 2.7.17 (test) / 2.14.18 (prod)
# MIGRATION 2.14: No changes needed. Simple scalar values.
#
# Purpose: Defines default input parameters for the etcd role, which validates
#          etcd cluster quorum, control-plane pod health, and ClusterOperator
#          etcd status before any cluster mutation or upgrade action.
# Dependencies: playbooks/vars/paths.yml, playbooks/vars/upgrade.yml
# Gate type: HARD
# ============================================================================

# Target cluster identifier (string, required)
cluster_name: ""

# OpenShift etcd namespace
etcd_namespace: "openshift-etcd"

# Minimum expected healthy etcd members / control-plane pods (default: 3 for HA)
etcd_expected_min_members: 3

# Master toggle for enforcing HARD gate failure on validation error
# Set to false by prevalidation aggregator during diagnostic sweeps before remediation
etcd_enforce_gate: true

# Transient retry parameters for oc CLI execution
oc_command_retries: 3
oc_command_retry_delay: 10
